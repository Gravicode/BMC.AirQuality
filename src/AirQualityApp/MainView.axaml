<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
			 mc:Ignorable="d"
             d:DesignWidth="800"
             d:DesignHeight="450"
             x:Class="AirQualityApp.MainView">
	<ScrollViewer>
		<StackPanel
                VerticalAlignment="Center"
                Margin="30"
                Spacing="30">
			<TextBlock FontSize="25">
				Air is the Most important things in ♥ Life
			</TextBlock>
			<TextBlock FontSize="14" Text="{Binding Desc}">
				
			</TextBlock>
			<StackPanel Orientation="Horizontal">
				<Button Content="Start" Command="{Binding OnStartCommand}"></Button>
				<Button Content="Stop" Command="{Binding OnStopCommand}"></Button>
			</StackPanel>
			<Slider />
		</StackPanel>
	</ScrollViewer>

</UserControl>